# LeakSanitizer suppression list. Every entry must have a corresponding ADR.
# Suppressions are for independently reproduced third-party process-lifetime
# allocations, never for hiding project-owned leaks.
#
# Debian 13 Qt xcb platform process-lifetime services object. See ADR-009.
leak:QGenericUnixServices::QGenericUnixServices
# Mesa llvmpipe worker/JIT process-lifetime allocations. See ADR-006.
leak:asan_thread_start
